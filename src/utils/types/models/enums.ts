export enum Role {
  EMPLOYER = "employer",
  JOB_SEEKER = "job_seeker",
}

export enum JobType {
  FULL_TIME = "full_time",
  PART_TIME = "part_time",
  CONTRACT = "contract",
}

export enum ApplicationStatus {
  Pending = "pending",
  Accepted = "accepted",
  Rejected = "rejected",
}